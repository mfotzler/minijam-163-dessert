import { DessertComponents } from "./types";

export const FrostingShot: DessertComponents = {
    movement: { killOnCollision: true },
    collision: { tiles: false },
    render: { spriteKey: 'coin' }
};
